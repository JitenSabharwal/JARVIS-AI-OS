global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/jarvis-alerts.yml

scrape_configs:
  - job_name: "jarvis-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["jarvis-api:8080"]
